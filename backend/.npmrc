# npm 配置文件
# 优化 Docker 构建过程

# 禁用可选依赖以避免架构兼容性问题
optional=false

# 使用更快的安装策略
prefer-offline=true
audit=false
fund=false

# 禁用进度条以减少日志输出
progress=false

# 设置超时时间
timeout=300000

# 禁用更新检查
update-notifier=false