# npm 配置文件
# 优化 Docker 构建过程

# 使用更快的安装策略
prefer-offline=false
audit=false
fund=false

# 禁用进度条以减少日志输出
progress=false

# 设置超时时间
timeout=600000

# 禁用更新检查
update-notifier=false

# 强制使用 npm 新语法
legacy-peer-deps=false

# 强制安装所有可选依赖
optional=true